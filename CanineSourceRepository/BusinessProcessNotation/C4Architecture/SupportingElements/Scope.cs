namespace CanineSourceRepository.BusinessProcessNotation.C4Architecture.SupportingElements;

//Its own thing, not under component - if anything unser system, but also used by tasks (remember c4 update)

public enum Scope {Internal, External}